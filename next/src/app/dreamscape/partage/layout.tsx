import { AppProvidersShell } from '@/components/AppProvidersShell'

export default function DreamscapePartageLayout({ children }: { children: React.ReactNode }) {
  return <AppProvidersShell>{children}</AppProvidersShell>
}
